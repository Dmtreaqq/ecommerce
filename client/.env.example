# Base URL of the NestJS API, and the origin product images load from.
# Defaults to http://localhost:3000 when unset — copy this file to .env.local
# only if you need to point at a different host.
VITE_API_URL=http://localhost:3000
